<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springprj.repository.BoardMapper">

    <insert id="insertBoard" parameterType="com.example.springprj.domain.Board">
            insert into board(board_no, board_writer, board_pw, board_content, board_viewCnt, board_regDate)
                            values(#{board_no}, #{board_writer}, #{board_pw}, #{board_content}, #{board_viewCnt}, now())
    </insert>






</mapper>